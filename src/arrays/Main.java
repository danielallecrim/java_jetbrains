package arrays;

public class Main {
    public static void main(String[] args) {

        // Create an instance of ArraysExample to use its methods
        ArraysExample example = new ArraysExample();

        // Each method demonstrates one concept about arrays
        example.createArray();          // Shows how to create and print arrays
        example.accessArray();          // Shows how to access elements by index
        example.modifyArray();          // Shows how to modify array values
        example.arrayLength();          // Shows how to get the array length
        example.multiDimensionalArray();// Shows how to use 2D arrays
    }
}
